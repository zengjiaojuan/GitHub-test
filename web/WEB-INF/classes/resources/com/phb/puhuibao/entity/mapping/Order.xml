<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.Order">
  <!-- RESOURCE RESULTMAP -->
  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.Order">
    <result property="orderId" column="order_id" jdbcType="INTEGER" />
    <result property="mUserId" column="M_USER_ID" jdbcType="INTEGER" />
    <result property="appreciationId" column="appreciation_id" jdbcType="INTEGER" />
    <result property="memberPrice" column="member_price" jdbcType="DECIMAL" />
    <result property="pay_status" column="payStatus" jdbcType="TINYINT" />
    <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="orderId" parameterType="com.phb.puhuibao.entity.Order">
    insert into phb_order (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.Order">
		update phb_order
		<trim prefix=" SET " prefixOverrides=",">
			<if test="price != null">
				  ,price=#{price,jdbcType=DECIMAL}
			</if>
			<if test="memberPrice != null">
				  ,member_price=#{memberPrice,jdbcType=DECIMAL}
			</if>
			<if test="number != null">
				  ,number=#{number,jdbcType=SMALLINT}
			</if>
			<if test="brokerage != null">
				  ,brokerage=#{brokerage,jdbcType=DECIMAL}
			</if>
			<if test="payStatus != null">
				  ,pay_status=#{payStatus,jdbcType=TINYINT}
			</if>
		</trim>
		where order_id=#{orderId}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.Order">
    delete from phb_order where order_id=#{orderId}
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_order <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_order <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_order <include refid="dynamicWhere" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_order where order_id=#{orderId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    m_user_id,
    appreciation_id,
    number,
    price,
    member_price,
    create_time
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,jdbcType=INTEGER},
			#{appreciationId,jdbcType=INTEGER},
			#{number,jdbcType=SMALLINT},
			#{price,jdbcType=DECIMAL},
			#{memberPrice,jdbcType=DECIMAL},
			#{createTime,jdbcType=TIMESTAMP}
	)
  </sql>
  <sql id="allColumn">
    order_id,
    m_user_id,
    appreciation_id,
    number,
    price,
    member_price,
    brokerage,
    pay_status
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="appreciationId != null">
			AND appreciation_Id = #{appreciationId}
		</if>
		<if test="mUserId != null">
			AND m_user_id = #{mUserId}
		</if>
		<if test="payStatus != null">
			AND pay_status = #{payStatus}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_order
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_order
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>