<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.UserCard">

  <!-- RESOURCE RESULTMAP -->

  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.UserCard">
	<result property="bankAccount" column="bank_account" jdbcType="VARCHAR" />
    <result property="mUserId" column="M_USER_ID" jdbcType="INTEGER" />
	<result property="bankPhone" column="bank_phone" jdbcType="VARCHAR" />
	<result property="prcptcd" column="prcptcd" jdbcType="CHAR" />
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" parameterType="com.phb.puhuibao.entity.UserCard">
    insert into phb_muser_card (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.UserCard">
		update phb_muser_card
		<trim prefix=" SET " prefixOverrides=",">
			<if test="mUserId != null">
				  ,m_user_id=#{mUserId,jdbcType=INTEGER}
			</if>
			<if test="bankPhone != null">
				  ,bank_phone=#{bankPhone,jdbcType=VARCHAR}
			</if>
			<if test="prcptcd != null">
				  ,prcptcd=#{prcptcd,jdbcType=CHAR}
			</if>
		</trim>
		where bank_account=#{bankAccount}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.UserCard">
    delete from phb_muser_card where bank_account=#{bankAccount}
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_muser_card <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_muser_card <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_muser_card <include refid="dynamicWhere" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_muser_card where bank_account=#{bankAccount}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    M_USER_ID,
	bank_phone,
	bank_account
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,jdbcType=INTEGER},
			#{bankPhone,jdbcType=VARCHAR},
			#{bankAccount,jdbcType=VARCHAR}
	)
  </sql>
  <sql id="allColumn">
    m_user_id,
	bank_phone,
	bank_account,
	prcptcd
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="mUserId != null">
			AND M_USER_ID = #{mUserId}
		</if>
		<if test="bankAccount != null">
			AND bank_account = #{bankAccount}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_muser_card
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_muser_card
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>	
  
</mapper>