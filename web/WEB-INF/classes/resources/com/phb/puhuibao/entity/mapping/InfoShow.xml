<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.InfoShow">
  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.InfoShow">
     
    <result property="infohowId"             column="infohow_id"            jdbcType="INTEGER" />
    <result property="firstColumn"          column="first_column"          jdbcType="VARCHAR" />
	<result property="secondColumn"         column="second_column"         jdbcType="VARCHAR" />
	<result property="infomationType"       column="infomation_type"       jdbcType="VARCHAR" />
 
  </resultMap>
  
  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true"   parameterType="com.phb.puhuibao.entity.InfoShow">
    insert into phb_muser_follow (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  
   
  
  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="java.util.Map">
    delete from phb_info_show <include refid="dynamicWhere" />
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> 
     from phb_info_show
    <include refid="dynamicWhere" />
    <include refid="dynamicOrderBy" />
    
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_muser_follow <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_muser_follow <include refid="dynamicWhere" /> <include refid="dynamicOrderBy" /> limit 1
  </select>
 
  
<update id="update" parameterType="com.phb.puhuibao.entity.InfoShow">
		update phb_muser_follow
		<trim prefix=" SET " prefixOverrides=",">
			<if test="followUname != null">
				  ,follow_uname=#{followUname,jdbcType=VARCHAR}
			</if>
			<if test="followReporttype != null">
				  ,follow_reporttype=#{followReporttype,jdbcType=VARCHAR}
			</if>
			<if test="followReportcomment != null">
				  ,follow_reportcomment=#{followReportcomment,jdbcType=VARCHAR}
			</if>
			<if test="followIsblocked != null">
				  ,follow_isblocked=#{followIsblocked,jdbcType=VARCHAR}
			</if>
		 		
			<if test="commentTime != null">
				  ,comment_time=#{commentTime,jdbcType=VARCHAR}
			</if>								 
		</trim>
		where m_user_id=#{muserId} and follow_user=#{followUser}
  </update>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
	m_user_id,
	follow_user,
	follow_uname,
	follow_time 
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{muserId,   jdbcType=VARCHAR},
			#{followUser, jdbcType=VARCHAR},
			#{followUname, jdbcType=VARCHAR},
			#{followTime,     jdbcType=TIMESTAMP} 
	      )
  </sql>
  <sql id="allColumn">
  
	 infohow_id,
	 first_column,
	 second_column,
	 infomation_type 
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="infomationType != null">
			AND infomation_type = #{infomationType}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_muser_follow
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
  	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_muser_follow
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>