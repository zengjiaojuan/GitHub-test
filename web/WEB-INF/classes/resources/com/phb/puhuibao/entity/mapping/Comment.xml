<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.Comment">
  <!-- RESOURCE RESULTMAP -->
  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.Comment">
    <id property="commentId" column="comment_id" jdbcType="INTEGER" />
    <result property="mUserId" column="m_user_id" jdbcType="INTEGER" />
    <result property="resourceId" column="resource_id" jdbcType="INTEGER" />
	<result property="content" column="content" jdbcType="VARCHAR" />
	<result property="userName" column="m_user_name" jdbcType="VARCHAR" />
    <result property="createTime" column="create_time" jdbcType="TIMESTAMP" />
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true"  parameterType="com.phb.puhuibao.entity.Comment">
    insert into phb_comment (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.Comment">
		update phb_comment
		<trim prefix=" SET " prefixOverrides=",">
			<if test="content != null">
				  ,content=#{content,jdbcType=VARCHAR}
			</if>
		</trim>
		where comment_id=#{commentId}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.Comment">
    delete from phb_comment where comment_id=#{commentId}
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_comment a left join phb_mobile_user c on a.m_user_id=c.m_user_id <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_comment a left join phb_mobile_user c on a.m_user_id=c.m_user_id <include refid="dynamicWhere" />
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_comment a left join phb_mobile_user c on a.m_user_id=c.m_user_id where comment_id=#{commentId}
  </select>
 <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_comment a left join phb_mobile_user c on a.m_user_id=c.m_user_id <include refid="dynamicWhere" /> limit 1
  </select>
  <!-- MySQL specific -->
  <sql id="allapplColumn">
    m_user_id,
    resource_id,
	content
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,jdbcType=INTEGER},
			#{resourceId,jdbcType=INTEGER},
			#{content,jdbcType=VARCHAR}
	)
  </sql>
  <sql id="allColumn">
    a.comment_id,
	a.content,
    a.m_user_id,
    c.nickname m_user_name,
    c.photo,
    a.create_time 
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="mUserId != null">
			AND a.m_user_id = #{mUserId}
		</if>
		<if test="resourceId != null">
			AND a.resource_id = #{resourceId}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by a.${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_comment a left join phb_mobile_user c on a.m_user_id=c.m_user_id
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_comment a left join phb_mobile_user c on a.m_user_id=c.m_user_id
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>	
  
</mapper>