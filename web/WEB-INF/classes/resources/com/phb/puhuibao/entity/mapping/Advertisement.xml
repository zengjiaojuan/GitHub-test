<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.Advertisement">

  <!-- RESOURCE RESULTMAP -->

  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.Advertisement">
    <result property="adId" column="ad_id" jdbcType="SMALLINT" />
    <result property="adName" column="ad_name" jdbcType="VARCHAR" />
    <result property="activity" column="activity" jdbcType="VARCHAR" />
    <result property="controller" column="controller" jdbcType="VARCHAR" />
    <result property="startDate" column="start_date" jdbcType="DATE" />
    <result property="endDate" column="end_date" jdbcType="DATE" />
    <result property="image" column="image" jdbcType="VARCHAR" />
    <result property="status" column="status" jdbcType="TINYINT"/>
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="adId" parameterType="com.phb.puhuibao.entity.Advertisement">
    insert into phb_advertisement (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.Advertisement">
		update phb_advertisement
		<trim prefix=" SET " prefixOverrides=",">
			<if test="adName != null">
				  ,ad_name=#{adName,jdbcType=INTEGER}
			</if>
			<if test="activity != null">
				  ,activity=#{activity,jdbcType=INTEGER}
			</if>
			<if test="controller != null">
				  ,controller=#{controller,jdbcType=INTEGER}
			</if>
			<if test="startDate != null">
				  ,start_date=#{startDate,jdbcType=INTEGER}
			</if>
			<if test="endDate != null">
				  ,end_date=#{endDate,jdbcType=INTEGER}
			</if>
			<if test="image != null">
				  ,image=#{image,jdbcType=INTEGER}
			</if>
			<if test="status != null">
				  ,status=#{status,jdbcType=DATE}
			</if>
		</trim>
		where ad_id=#{adId}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.Advertisement">
    delete from phb_advertisement where ad_id=#{adId}
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_advertisement <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_advertisement <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_advertisement <include refid="dynamicWhere" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_advertisement where ad_id=#{adId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    ad_name,
    activity,
    controller,
    start_date,
    end_date,
    image,
    status
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{adName,jdbcType=VARCHAR},
			#{activity,jdbcType=VARCHAR},
			#{controller,jdbcType=VARCHAR},
			#{startDate,jdbcType=DATE},
			#{endDate,jdbcType=DATE},
			#{image,jdbcType=VARCHAR},
			#{status,jdbcType=TINYINT}
	)
  </sql>
  <sql id="allColumn">
    ad_id,
    ad_name,
    activity,
    controller,
    start_date,
    end_date,
    image,
    status
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="status != null">
			AND status = #{status}
		</if>
		<if test="gEndDate != null">
			AND end_date <![CDATA[>]]>= #{gEndDate}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_advertisement
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
  	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_advertisement
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>