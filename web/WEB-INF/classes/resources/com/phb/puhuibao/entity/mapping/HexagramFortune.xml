<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.HexagramFortune">
  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.HexagramFortune">
    <result property="mUserId"                   column="m_user_id"                  jdbcType="INTEGER" />
    <result property="isBind"                    column="is_bind"                    jdbcType="DECIMAL" />
	<result property="depositValue"              column="deposit_value"              jdbcType="DECIMAL" />
	<result property="messionPublish"            column="mession_publish"            jdbcType="DECIMAL" />
	<result property="investValue"               column="invest_value"               jdbcType="DECIMAL" />
 
  </resultMap>
  
  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true"   parameterType="com.phb.puhuibao.entity.HexagramFortune">
    insert into phb_hexagram_fortune (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  
   
  
  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="java.util.Map">
    delete from phb_hexagram_fortune <include refid="dynamicWhere" />
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> 
    from phb_hexagram_fortune 
     
    <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_hexagram_fortune <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_hexagram_fortune <include refid="dynamicWhere" /> <include refid="dynamicOrderBy" /> limit 1
  </select>
   <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select * from phb_hexagram_fortune where m_user_id=#{mUserId}
  </select>
  
<update id="update" parameterType="com.phb.puhuibao.entity.HexagramFortune">
		update phb_hexagram_fortune
		<trim prefix=" SET " prefixOverrides=",">
			<if test="isBind != null">
				  ,is_bind=#{isBind,jdbcType=DECIMAL}
			</if>
			<if test="depositValue != null">
				  ,deposit_value=#{depositValue,jdbcType=DECIMAL}
			</if>
			<if test="messionPublish != null">
				  ,mession_publish=#{messionPublish,jdbcType=DECIMAL}
			</if>
			<if test="investValue != null">
				  ,invest_value=#{investValue,jdbcType=DECIMAL}
			</if>
			 
			 							 
		</trim>
		where m_user_id=#{mUserId} 
  </update>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
	m_user_id  
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,                    jdbcType=INTEGER} 
	      )
  </sql>
  <sql id="allColumn">
  
	m_user_id,
	is_bind,
	deposit_value,
	mession_publish,
	invest_value 
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="mUserId != null">
			AND m_user_id = #{mUserId}
		</if>
		 
 
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_hexagram_fortune
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
  	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_hexagram_fortune
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>