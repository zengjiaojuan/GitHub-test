<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.ResourceOrder">
  <!-- RESOURCE RESULTMAP -->
  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.ResourceOrder">
    <result property="orderId" column="order_id" jdbcType="INTEGER" />
    <result property="mUserId" column="m_user_id" jdbcType="INTEGER" />
    <result property="resourceId" column="resource_id" jdbcType="INTEGER" />
    <result property="price" column="price" jdbcType="DECIMAL" />
    <result property="status" column="status" jdbcType="TINYINT" />
    <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    <result property="content" column="content" jdbcType="VARCHAR" />
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="orderId" parameterType="com.phb.puhuibao.entity.ResourceOrder">
    insert into phb_resource_order (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.ResourceOrder">
		update phb_resource_order
		<trim prefix=" SET " prefixOverrides=",">
			<if test="price != null">
				  ,price=#{price,jdbcType=DECIMAL}
			</if>
			<if test="status != null">
				  ,status=#{status,jdbcType=TINYINT}
			</if>
			<if test="content != null">
				  ,content=#{content,jdbcType=VARCHAR}
			</if>
		</trim>
		where order_id=#{orderId}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.ResourceOrder">
    delete from phb_resource_order where order_id=#{orderId}
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_resource_order <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_resource_order <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_resource_order <include refid="dynamicWhere" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_resource_order where order_id=#{orderId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    m_user_id,
    resource_id,
    status,
    create_time,
    content
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,jdbcType=INTEGER},
			#{resourceId,jdbcType=INTEGER},
			#{status,jdbcType=TINYINT},
			#{createTime,jdbcType=TIMESTAMP},
			#{content,jdbcType=VARCHAR}
	)
  </sql>
  <sql id="allColumn">
    order_id,
    m_user_id,
    resource_id,
    price,
    status,
    content,
    create_time
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="resourceId != null">
			AND resource_id = #{resourceId}
		</if>
		<if test="mUserId != null">
			AND m_user_id = #{mUserId}
		</if>
		<if test="status != null">
			AND status = #{status}
		</if>
		<if test="gstatus != null">
			AND status <![CDATA[>]]> #{gstatus}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_resource_order
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_resource_order
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>