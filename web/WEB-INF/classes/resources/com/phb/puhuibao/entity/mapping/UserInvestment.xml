<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.UserInvestment">

  <!-- RESOURCE RESULTMAP -->

  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.UserInvestment">
    <id property="investmentId" column="investment_id" jdbcType="INTEGER" />
    <result property="mUserId" column="M_USER_ID" jdbcType="INTEGER" />
	<result property="bidSN" column="bid_sn" jdbcType="VARCHAR" />
	<result property="productSN" column="product_sn" jdbcType="VARCHAR" />
    <result property="investmentAmount" column="investment_amount" jdbcType="BIGINT" />
    <result property="createTime" column="create_time" jdbcType="TIMESTAMP" />
    <result property="incomeDate" column="income_date" jdbcType="DATE" />
    <result property="lastIncome" column="last_income" jdbcType="DECIMAL" />
    <result property="productName" column="product_name" jdbcType="VARCHAR"/>
    <result property="guaranteeMethod" column="guarantee_method" jdbcType="VARCHAR"/>
    <result property="lastDate" column="last_date" jdbcType="DATE" />
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="investmentId" parameterType="com.phb.puhuibao.entity.UserInvestment">
    insert into phb_muser_investment (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.UserInvestment">
		update phb_muser_investment
		<trim prefix=" SET " prefixOverrides=",">
			<if test="bidSN != null">
				  ,bid_sn=#{bidSN,jdbcType=VARCHAR}
			</if>
			<if test="investmentAmount != null">
				  ,investment_amount=#{investmentAmount,jdbcType=BIGINT}
			</if>
			<if test="lastIncome != null">
				  ,last_income=#{lastIncome,jdbcType=DECIMAL}
			</if>
			<if test="status != null">
				  ,status=#{status,jdbcType=TINYINT}
			</if>
			<if test="lastDate != null">
				  ,last_date=#{lastDate,jdbcType=DATE}
			</if>
		</trim>
		where investment_id=#{investmentId}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.UserInvestment">
    delete from phb_muser_investment <include refid="dynamicWhere" />
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_muser_investment a left join phb_product_bid c on a.bid_sn= c.bid_sn left join phb_asset_product b on b.product_sn= c.product_sn <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_muser_investment a left join phb_product_bid c on a.bid_sn= c.bid_sn left join phb_asset_product b on b.product_sn= c.product_sn <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_muser_investment a left join phb_product_bid c on a.bid_sn= c.bid_sn left join phb_asset_product b on b.product_sn= c.product_sn <include refid="dynamicWhere" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_muser_investment a left join phb_product_bid c on a.bid_sn= c.bid_sn left join phb_asset_product b on b.product_sn= c.product_sn where investment_id=#{investmentId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    M_USER_ID,
	bid_sn,
	investment_amount,
	create_time,
	status,
	income_date
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,jdbcType=INTEGER},
			#{bidSN,jdbcType=VARCHAR},
			#{investmentAmount,jdbcType=BIGINT},
			#{createTime,jdbcType=TIMESTAMP},
			#{status,jdbcType=TINYINT},
			#{incomeDate,jdbcType=DATE}
	)
  </sql>
  <sql id="allColumn">
    a.investment_id,
    a.m_user_id,
	a.bid_sn,
	a.investment_amount,
	a.income_date,
	a.last_income,
	a.status,
	b.product_sn,
	b.product_name,
	b.guarantee_method,
	b.period,
	b.unit,
	a.create_time,
	a.last_date
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="mUserId != null">
			AND a.m_user_id = #{mUserId}
		</if>
		<if test="status != null">
			AND a.status = #{status}
		</if>
		<if test="bidSN != null">
			AND a.bid_sn = #{bidSN}
		</if>
		<if test="productSN != null">
			AND b.product_sn = #{productSN}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by a.${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_muser_investment a left join phb_product_bid c on a.bid_sn= c.bid_sn left join phb_asset_product b on b.product_sn= c.product_sn
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_muser_investment a left join phb_product_bid c on a.bid_sn= c.bid_sn left join phb_asset_product b on b.product_sn= c.product_sn
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>	
  
</mapper>