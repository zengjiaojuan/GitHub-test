<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.MonthBalance">
  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.MonthBalance">
    <result property="yyyymm" column="yyyymm" jdbcType="VARCHAR" />
    <result property="payAmount" column="pay_amount" jdbcType="DECIMAL" />
    <result property="investmentIncome" column="investment_income" jdbcType="DECIMAL" />
    <result property="loanInterest" column="loan_interest" jdbcType="DECIMAL" />
    <result property="totalAsset" column="total_asset" jdbcType="DECIMAL" />
    <result property="totalAssetLast" column="total_asset_last" jdbcType="DECIMAL" />
    <result property="withdrawAmount" column="withdraw_amount" jdbcType="DECIMAL" />
    <result property="balance" column="balance" jdbcType="DECIMAL" />
  </resultMap>
  
  <!-- RESOURCE INSERT -->
  <insert id="insert" parameterType="com.phb.puhuibao.entity.MonthBalance">
    insert into phb_month_balance (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_month_balance <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_month_balance <include refid="dynamicWhere" />
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_month_balance where yyyymm=#{yyyymm}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    yyyymm,
    pay_amount,
    investment_income,
    loan_interest,
    total_asset,
    total_asset_last,
    withdraw_amount,
    balance,
    status
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{yyyymm,jdbcType=VARCHAR},
			#{payAmount,jdbcType=DECIMAL},
			#{investmentIncome,jdbcType=DECIMAL},
			#{loanInterest,jdbcType=DECIMAL},
			#{totalAsset,jdbcType=DECIMAL},
			#{totalAssetLast,jdbcType=DECIMAL},
			#{withdrawAmount,jdbcType=DECIMAL},
			#{balance,jdbcType=DECIMAL},
			#{status,jdbcType=INTEGER}
	)
  </sql>
  <sql id="allColumn">
    yyyymm,
    pay_amount,
    investment_income,
    loan_interest,
    total_asset,
    total_asset_last,
    withdraw_amount,
    balance,
    status
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="yyyymm != null">
			AND yyyymm = #{yyyymm}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_month_balance
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
  	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_month_balance
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>