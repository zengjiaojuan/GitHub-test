<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.ExperienceInvestment">

  <!-- RESOURCE RESULTMAP -->

  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.ExperienceInvestment">
    <id property="investmentId" column="investment_id" jdbcType="INTEGER" />
    <result property="mUserId" column="M_USER_ID" jdbcType="INTEGER" />
	<result property="productSN" column="product_sn" jdbcType="VARCHAR" />
    <result property="investmentAmount" column="investment_amount" jdbcType="BIGINT" />
    <result property="createTime" column="create_time" jdbcType="TIMESTAMP" />
    <result property="incomeDate" column="income_date" jdbcType="DATE" />
    <result property="lastIncome" column="last_income" jdbcType="DECIMAL" />
    <result property="productName" column="product_name" jdbcType="VARCHAR"/>
    <result property="annualizedRate" column="annualized_rate" jdbcType="DECIMAL"/>
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="investmentId" parameterType="com.phb.puhuibao.entity.ExperienceInvestment">
    insert into phb_muser_experience_investment (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.ExperienceInvestment">
		update phb_muser_experience_investment
		<trim prefix=" SET " prefixOverrides=",">
			<if test="productSN != null">
				  ,product_sn=#{productSN,jdbcType=VARCHAR}
			</if>
			<if test="investmentAmount != null">
				  ,investment_amount=#{investmentAmount,jdbcType=BIGINT}
			</if>
			<if test="lastIncome != null">
				  ,last_income=#{lastIncome,jdbcType=DECIMAL}
			</if>
			<if test="status != null">
				  ,status=#{status,jdbcType=TINYINT}
			</if>
		</trim>
		where investment_id=#{investmentId}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.ExperienceInvestment">
    delete from phb_muser_experience_investment <include refid="dynamicWhere" />
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_muser_experience_investment a left join phb_experience_product b on a.product_sn= b.product_sn <include refid="dynamicWhere" /><include refid="dynamicOrderBy" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_muser_experience_investment a left join phb_experience_product b on a.product_sn= b.product_sn <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_muser_experience_investment a left join phb_experience_product b on a.product_sn= b.product_sn <include refid="dynamicWhere" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_muser_experience_investment a left join phb_experience_product b on a.product_sn= b.product_sn where investment_id=#{investmentId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    M_USER_ID,
	product_sn,
	investment_amount,
	create_time,
	status,
	income_date
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,jdbcType=INTEGER},
			#{productSN,jdbcType=VARCHAR},
			#{investmentAmount,jdbcType=BIGINT},
			#{createTime,jdbcType=TIMESTAMP},
			#{status,jdbcType=TINYINT},
			#{incomeDate,jdbcType=DATE}
	)
  </sql>
  <sql id="allColumn">
    a.investment_id,
    a.m_user_id,
	a.product_sn,
	a.investment_amount,
	a.income_date,
	a.last_income,
	a.status,
	b.product_name,
	b.period,
	b.annualized_rate
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="mUserId != null">
			AND M_USER_ID = #{mUserId}
		</if>
		<if test="leStatus != null">
			AND status <![CDATA[<]]>= #{leStatus}
		</if>
		<if test="status != null">
			AND status = #{status}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by a.${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_muser_experience_investment a left join phb_experience_product b on a.product_sn= b.product_sn
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_muser_experience_investment
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>	
  
</mapper>