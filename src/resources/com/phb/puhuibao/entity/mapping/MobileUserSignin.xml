<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.MobileUserSignin">

  <!-- RESOURCE RESULTMAP -->

  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.MobileUserSignin">
    <result property="mUserId" column="m_user_id" jdbcType="INTEGER" />
    <result property="totalIntegral" column="total_integral" jdbcType="INTEGER" />
    <result property="usedIntegral" column="used_integral" jdbcType="INTEGER" />
    <result property="signinDate" column="signin_date" jdbcType="DATE" />
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" parameterType="com.phb.puhuibao.entity.MobileUserSignin">
    insert into phb_mobile_user_signin (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.MobileUserSignin">
		update phb_mobile_user_signin
		<trim prefix=" SET " prefixOverrides=",">
			<if test="totalIntegral != null">
				  ,total_integral=#{totalIntegral,jdbcType=INTEGER}
			</if>
			<if test="usedIntegral != null">
				  ,used_integral=#{usedIntegral,jdbcType=INTEGER}
			</if>
			<if test="signinDate != null">
				  ,signin_date=#{signinDate,jdbcType=DATE}
			</if>
		</trim>
		where m_user_id=#{mUserId}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.MobileUserSignin">
    delete from phb_mobile_user_signin where m_user_id=#{mUserId}
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_mobile_user_signin <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_mobile_user_signin <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_mobile_user_signin <include refid="dynamicWhere" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_mobile_user_signin where m_user_id=#{mUserId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    m_user_id,
    total_integral,
    signin_date
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,jdbcType=INTEGER},
			#{totalIntegral,jdbcType=INTEGER},
			#{signinDate,jdbcType=DATE}
	)
  </sql>
  <sql id="allColumn">
    m_user_id,
    total_integral,
    used_integral,
    signin_date
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="mUserId != null">
			AND m_user_id = #{mUserId}
		</if>
		<if test="signinDate != null">
			AND signin_date = #{signinDate}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_mobile_user_signin
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
  	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_mobile_user_signin
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>