<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.UserExperience">

  <!-- RESOURCE RESULTMAP -->

  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.UserExperience">
    <result property="experienceId" column="experience_id" jdbcType="INTEGER" />
    <result property="mUserId" column="m_user_id" jdbcType="INTEGER" />
    <result property="experienceAmount" column="experience_amount" jdbcType="INTEGER" />
    <result property="status" column="status" jdbcType="TINYINT" />
    <result property="lastDate" column="last_date" jdbcType="TIMESTAMP"/>
    <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
  </resultMap>

  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="experienceId" parameterType="com.phb.puhuibao.entity.UserExperience">
    insert into phb_muser_experience (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.UserExperience">
		update phb_muser_experience
		<trim prefix=" SET " prefixOverrides=",">
			<if test="experienceAmount != null">
				  ,experience_amount=#{experienceAmount,jdbcType=INTEGER}
			</if>
			<if test="status != null">
				  ,status=#{status,jdbcType=TINYINT}
			</if>
		</trim>
		where experience_id=#{experienceId}
  </update>

  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="com.phb.puhuibao.entity.UserExperience">
    delete from phb_muser_experience where experience_id = #{experienceId}
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_muser_experience <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_muser_experience <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_muser_experience <include refid="dynamicWhere" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select <include refid="allColumn" /> from phb_muser_experience where experience_id=#{experienceId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    m_user_id,
    experience_amount,
    status,
    last_date
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{mUserId,jdbcType=INTEGER},
			#{experienceAmount,jdbcType=INTEGER},
			#{status,jdbcType=TINYINT},
			#{lastDate,jdbcType=TIMESTAMP}
	)
  </sql>
  <sql id="allColumn">
    experience_id,
    m_user_id,
    experience_amount,
    status,
    last_date
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="gelastDate != null">
			AND last_date <![CDATA[>]]>= #{gelastDate}
		</if>
		<if test="lastDate != null">
			AND last_date = #{lastDate}
		</if>
		<if test="mUserId != null">
			AND m_user_id = #{mUserId}
		</if>
		<if test="status != null">
			AND status = #{status}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_muser_experience
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_muser_experience
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>	
  
</mapper>