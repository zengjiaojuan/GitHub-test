<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.ThirdPayLog">
  <!-- RESOURCE RESULTMAP -->
  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.ThirdPayLog">
    <result property="logId" column="log_id" jdbcType="CHAR" />
    <result property="action" column="action" jdbcType="VARCHAR" />
    <result property="params" column="action" jdbcType="LONGVARCHAR" />
  </resultMap>
  
  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="logId" parameterType="com.phb.puhuibao.entity.ThirdPayLog">
    insert into phb_third_pay_log (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  
  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="java.util.Map">
    delete from phb_third_pay_log where log_id=#{logId}
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_third_pay_log <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_third_pay_log <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_third_pay_log <include refid="dynamicWhere" /> <include refid="dynamicOrderBy" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select * from phb_third_pay_log where log_id=#{logId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
    log_id,
    action,
	params,
	error,
	status
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{logId,jdbcType=CHAR},
			#{action,jdbcType=VARCHAR},
			#{params,jdbcType=VARCHAR},
			#{error,jdbcType=VARCHAR},
			#{status,jdbcType=TINYINT}
	)
  </sql>
  <sql id="allColumn">
	log_id,
	action,
	params,
	status
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="logId != null">
			AND log_id = #{logId}
		</if>
		<if test="action != null">
			AND action = #{action}
		</if>
		<if test="params != null">
			AND params = #{params}
		</if>
		<if test="status != null">
			AND status = #{status}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_third_pay_log
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
  	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_third_pay_log
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>