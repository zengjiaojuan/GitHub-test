<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.phb.puhuibao.entity.Notification">
  <resultMap id="resourceResultMap" type="com.phb.puhuibao.entity.Notification">
    <result property="notificationId" column="notification_id" jdbcType="INTEGER" />
    <result property="title" column="title" jdbcType="VARCHAR" />
	<result property="content" column="content" jdbcType="VARCHAR" />
	<result property="status" column="status" jdbcType="TINYINT" />
    <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
  </resultMap>
  
  <!-- RESOURCE INSERT -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="notificationId" parameterType="com.phb.puhuibao.entity.Notification">
    insert into phb_notification (<include refid="allapplColumn"/>) <include refid="allapplColumnVar"/>
  </insert>
  
  <!-- RESOURCE UPDATE -->
  <update id="update" parameterType="com.phb.puhuibao.entity.Notification">
		update phb_notification
		<trim prefix=" SET " prefixOverrides=",">
			<if test="title != null">
				  ,title=#{title,jdbcType=VARCHAR}
			</if>
			<if test="content != null">
				  ,content=#{content,jdbcType=VARCHAR}
			</if>
			<if test="status != null">
				  ,status=#{status,jdbcType=TINYINT}
			</if>
		</trim>
		where notification_id=#{notificationId}
  </update>
  
  <!-- RESOURCE DELETE -->
  <delete id="delete" parameterType="java.util.Map">
    delete from phb_notification <include refid="dynamicWhere" />
  </delete>
  
  <!-- RESOURCE SELECT -->
  <select id="query" parameterType="java.util.Map" resultMap="resourceResultMap">
    select <include refid="allColumn" /> from phb_notification <include refid="dynamicWhere" />
  </select>
  <select id="findByPager" parameterType="java.util.Map" resultMap="resourceResultMap">
    <include refid="queryBody" />
  </select>
  <select id="findByPagerCount" parameterType="java.util.Map" resultType="int" >
	select count(1) from phb_notification <include refid="dynamicWhere" />
  </select>
  <select id="unique" parameterType="java.util.Map" resultMap="resourceResultMap" >
	select <include refid="allColumn" /> from phb_notification <include refid="dynamicWhere" /> <include refid="dynamicOrderBy" /> limit 1
  </select>
  <select id="getById" parameterType="java.util.Map" resultMap="resourceResultMap">
	select * from phb_notification where version_id=#{versionId}
  </select>

  <!-- MySQL specific -->
  <sql id="allapplColumn">
	title,
	content,
	status
  </sql>
  <sql id="allapplColumnVar">
	values(
			#{title,jdbcType=VARCHAR},
			#{content,jdbcType=VARCHAR},
			#{status,jdbcType=TINYINT}
	)
  </sql>
  <sql id="allColumn">
    notification_id,
	title,
	content,
	status,
    create_time
  </sql>
  <sql id="dynamicWhere">
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="notificationId != null">
			AND notification_id = #{notificationId}
		</if>
		<if test="title != null">
			AND title = #{title}
		</if>
		<if test="content != null">
			AND content = #{content}
		</if>
		<if test="status != null">
			AND status = #{status}
		</if>
	</trim>
  </sql>
  <sql id="dynamicOrderBy">
	<if test="orderBy!=null">
		order by ${orderBy}
		<if test="order!=null">
			${order}
		</if>
	</if>
  </sql>

  <sql id="queryBody">
	<if test="sno!=null and eno!=null and orderBy!=null">
		select <include refid="allColumn" /> from phb_notification
		<include refid="dynamicWhere" />
		<include refid="dynamicOrderBy" />
		limit ${sno-1},${eno}
	</if>
  	<!-- 无orderby -->
	<if test="sno!=null and eno!=null and orderBy==null">
		select <include refid="allColumn" /> from phb_notification
		<include refid="dynamicWhere" />
		limit ${sno-1},${eno}
	</if>
  </sql>
</mapper>